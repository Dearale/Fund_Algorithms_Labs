#include "../include/statusManager.h"
#include <stdio.h>

void printStatus(Status status) {
    switch (status) {
        case LINE_OUT_OF_BOUNDS:
            break;
        default:
            break;
    }
}